# Never commit credentials
.env
builder/.env
*.env

# ops.html contains hardcoded API keys — local use only, never push
ops.html

# macOS
.DS_Store

# Social media content — local only, do not push to GitHub
social/
assets/social/*.png
assets/social/*.jpg
marketing/

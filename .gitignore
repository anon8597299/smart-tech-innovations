# Never commit credentials
.env
builder/.env
*.env

# ops.html and admin.html contain hardcoded API keys — local use only, never push
ops.html
admin.html

# macOS
.DS_Store

# Social media images — local only
assets/social/*.png
assets/social/*.jpg
marketing/

# Social data files — push these (queue, intel, leads seed)
# social/ directory is NOT ignored — only the images above are
.run.pid
